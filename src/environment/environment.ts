import { Enemy } from '../actors';
import { Point } from '../models';

export const BOUNDS: Point = {x: 20, y: 15};

export let EnemyList: Enemy[] = [];

export const green: string = 'rgba(3, 252, 90)';
